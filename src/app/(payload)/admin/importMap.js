/** @type import('payload').ImportMap */
// Seed import map — Payload regenerates this file on first boot to register
// admin UI components used by the config (rich-text features, custom cells,
// etc.). Safe to commit as-is; it is overwritten automatically.
export const importMap = {}
